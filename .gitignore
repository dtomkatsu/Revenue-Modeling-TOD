.DS_Store
node_modules/
*.log
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ETL caches (raw fetched data, intermediates) — final outputs are committed
data/raw/
data/cache/
data/processed/
# Planetiler auxiliary downloads + tile weights (rebuildable, ~1.3 GB)
data/sources/
data/tile_weights.tsv.gz

# Regression snapshots used during pipeline rework (e.g. CIP integration).
data/_*_snapshot.json
